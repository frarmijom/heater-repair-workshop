meta {
  name: Create repair order
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/repair-orders
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {"id":"ORDER-001","customerContact":"+56911112222"}
}

tests {
  test("returns 201", function () {
    expect(res.getStatus()).to.equal(201);
  });
}
