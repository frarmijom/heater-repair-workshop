meta {
  name: List repair orders
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/repair-orders
  body: none
  auth: none
}

tests {
  test("returns the created order", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()[0].id).to.equal(bru.getVar("orderId"));
  });
}
