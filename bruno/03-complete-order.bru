meta {
  name: Complete repair order
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/repair-orders/ORDER-001/complete
  body: none
  auth: none
}

tests {
  test("returns a completed order", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().status).to.equal("COMPLETED");
  });
}
